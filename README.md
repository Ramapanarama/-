telegram Chess Puzzle Bot

Бот для Telegram, который присылает шахматные задачи — как ежедневные с Lichess, так и случайные из локального архива.
Пользователь может проверять свои ходы, получать подсказки, смотреть решение и вести личную статистику успехов.

🚀 Возможности

/daily — ежедневная задача с Lichess
Если Lichess недоступен, бот автоматически подставляет локальную задачу.

/puzzle — случайная задача из локального тестового набора.

/stats — личная статистика: решено / провалено, процент успеха.

Подсказки и решение через inline-кнопки.

Проверка хода пользователя по FEN-позиции.

Локальное хранение состояния задач и статистики внутри сессии бота.

🧠 Локальный тестовый набор

На случай, если API Lichess не отвечает, в коде предусмотрено несколько FEN-позиций с однозначным решением — это обеспечивает стабильную работу бота всегда.

🛠️ Установка и запуск

Установите зависимости:

pip install pyTelegramBotAPI requests


Поместите токен Telegram-бота в переменную:

export TELEGRAM_API_TOKEN="ВАШ_ТОКЕН"


(в коде он пока жёстко прописан — рекомендуется вынести в среду)

Запустите бота:

python liChess.py


Бот сразу начнёт слушать новые сообщения.

📡 Используемые API

Lichess Daily Puzzle API
https://lichess.org/api/puzzle/daily

Используется для получения свежей ежедневной задачи.

Telegram Bot API
Через библиотеку telebot.

📁 Структура

Файл liChess.py содержит:

обработчики команд /start, /daily, /puzzle, /stats;

callback-кнопки;

отправку FEN-позиций с клавиатурой;

механизм подсказок;

проверку хода пользователя;

fallback-логку для офлайн-режима.
Шахматный бот
